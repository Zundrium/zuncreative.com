import { PlayInViewDirective } from './play-in-view.directive';

describe('PlayInViewDirective', () => {
  it('should create an instance', () => {
    const directive = new PlayInViewDirective();
    expect(directive).toBeTruthy();
  });
});
